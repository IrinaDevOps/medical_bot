import os
from pathlib import Path
from dotenv import load_dotenv

# Загружаем переменные из .env файла
load_dotenv()

# Telegram Bot Token (ОБЯЗАТЕЛЬНО установить в .env или переменных окружения)
BOT_TOKEN = os.getenv("BOT_TOKEN", "YOUR_BOT_TOKEN_HERE")

# Суперадминистратор (Telegram ID)
# Узнать свой ID можно через @userinfobot
SUPERADMIN_ID = int(os.getenv("SUPERADMIN_ID", "0"))

# Пути к файлам данных
BASE_DIR = Path(__file__).parent
DATA_DIR = BASE_DIR / "data"

# SQLite база данных
DATABASE_FILE = DATA_DIR / "medical_bot.db"

# JSON файлы (для обратной совместимости и миграции)
PATIENTS_FILE = DATA_DIR / "patients.json"
STAFF_FILE = DATA_DIR / "staff.json"
REMINDERS_FILE = DATA_DIR / "reminders.json"

# Создаем директорию data если её нет
DATA_DIR.mkdir(exist_ok=True)

# Роли персонала
ROLES = {
    "admin": "Администратор",
    "doctor": "Врач"
}

# Часовой пояс (по умолчанию Красноярск)
TIMEZONE = "Asia/Krasnoyarsk"

# Отделения больницы
DEPARTMENTS = [
    "Хирургическое отделение",
    "Урологическое отделение №1",
    "Урологическое отделение №2",
    "Гинекологическое отделение №1",
    "Гинекологическое отделение №2",
    "Гинекологическое отделение №3",
    "Гинекологическое отделение №4",
    "Дневной стационар"
]

# Интервалы напоминаний (в днях после операции)
REMINDER_INTERVALS = [5, 10, 30]

# День автоудаления пациента (после операции)
AUTO_DELETE_DAY = 31

# Шаблоны напоминаний по умолчанию
DEFAULT_REMINDER_TEMPLATES = {
    5: (
        "Прошло 5 дней после операции.\n\n"
        "Как ваше самочувствие? Пожалуйста, сообщите нам о вашем состоянии.\n\n"
        "Нажмите кнопку ниже, чтобы отправить отчет."
    ),
    10: (
"Прошло 10 дней после операции.\n\n"
        "Как ваше самочувствие? Пожалуйста, сообщите нам о вашем состоянии.\n\n"
        "Нажмите кнопку ниже, чтобы отправить отчет."
    ),
    30: (
        "Прошло 30 дней после операции.\n\n"
        "Как ваше самочувствие? Пожалуйста, сообщите нам о вашем состоянии.\n\n"
        "Нажмите кнопку ниже, чтобы отправить отчет."
    )
}